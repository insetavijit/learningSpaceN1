## 🔹 1.1 HTML Document Fundamentals & Setup

- **DOCTYPE** : `<!DOCTYPE html>`
    
- **Root Element** : `<html lang="">`
    
- **Metadata Container** : `<head>`
    
- **Character Encoding** : `<meta charset="utf-8">`
    
- **Viewport** : `<meta name="viewport" content="width=device-width, initial-scale=1">`
    
- **Title** : `<title>`
    
- **External Resources** : `<link rel="stylesheet">`, favicons
    
- **Meta Tags** : description, keywords, author, robots
    
- **Base URL** : `<base href="">`
    
- **Inline Styles** : `<style>`
    
- **Scripts** : `<script>`, `defer`, `async`
    
- **No-Script Fallback** : `<noscript>`
    
- **Polyfills / HTML5 Shiv** : legacy browser support
    
- **Structure Philosophy** : semantic vs non-semantic (`<div>`)
    
- **Accessibility Basics** : `lang`, `aria-*`
    
- **Compatibility** : modern browsers, fallbacks
    

---

## 🔹 1.2 Document Structure & Sectioning

- **Sectioning Roots** : `<body>`, `<blockquote>`, `<figure>`, `<td>`
    
- **Landmarks** : `<header>`, `<nav>`, `<main>`, `<footer>`, `<aside>`
    
- **Content Sections** : `<section>`, `<article>`
    
- **Heading Hierarchy** : `<h1>`–`<h6>`, `<hgroup>`
    
- **Contact Info** : `<address>`
    
- **Grouping Content** : `<div>`, `<p>`, `<hr>`, `<pre>`, `<blockquote>`
    
- **Figures** : `<figure>`, `<figcaption>`
    
- **Disclosure Widgets** : `<details>`, `<summary>`
    
- **Dialog** : `<dialog>`
    
- **Semantic Flow** : outline algorithm, logical order
    

---

## 🔹 1.3 Text Content & Inline Semantics

- **Paragraphs & Breaks** : `<p>`, `<br>`, `<wbr>`
    
- **Headings** : `<h1>`–`<h6>`
    
- **Emphasis & Importance** : `<em>`, `<strong>`
    
- **Stylistic Alternatives** : `<i>`, `<b>`, `<u>`, `<s>`
    
- **Small & Highlight** : `<small>`, `<mark>`
    
- **Insert / Delete** : `<ins>`, `<del>`
    
- **Citations & Quotes** : `<cite>`, `<blockquote>`, `<q>`
    
- **Abbreviations & Terms** : `<abbr>`, `<dfn>`
    
- **Code & Keyboard** : `<code>`, `<kbd>`, `<samp>`, `<var>`, `<pre>`
    
- **Sub / Superscript** : `<sub>`, `<sup>`
    
- **Time & Dates** : `<time>`
    
- **Ruby Annotations** : `<ruby>`, `<rt>`, `<rp>`
    
- **Bidirectional Text** : `<bdi>`, `<bdo>`
    
- **Machine Data** : `<data>`
    
- **Generic Inline** : `<span>`
    

---

## 🔹 1.4 Lists & Tabular Data

- **Unordered Lists** : `<ul>`, `<li>`
    
- **Ordered Lists** : `<ol start reversed type>`, `<li>`
    
- **Description Lists** : `<dl>`, `<dt>`, `<dd>`
    
- **Menu** : `<menu>`
    
- **List Accessibility** : semantics, nesting, roles
    
- **Tables Core** : `<table>`, `<caption>`
    
- **Columns** : `<colgroup>`, `<col>`
    
- **Sections** : `<thead>`, `<tbody>`, `<tfoot>`
    
- **Rows & Cells** : `<tr>`, `<th>`, `<td>`
    
- **Table Accessibility** : `scope`, `headers`, summaries
    
- **Responsive Tables** : wrappers, CSS overflow
    

---

## 🔹 1.5 Links, Navigation & Embedded Content

- **Hyperlinks** : `<a href target download rel>`
    
- **Navigation Section** : `<nav>`
    
- **Image Maps** : `<map>`, `<area>`
    
- **External Embeds** : `<iframe>`, `<embed>`, `<object>`, `<param>`
    
- **Images** : `<img>`, `<picture>`, `<source>`
    
- **Audio / Video** : `<audio>`, `<video>`, `<track>`
    
- **Media Figures** : `<figure>`, `<figcaption>`
    
- **Canvas** : `<canvas>`
    
- **SVG / MathML** : `<svg>`, `<math>`
    
- **Responsive Media** : `srcset`, `sizes`, art direction
    

---

## 🔹 1.6 Forms & Interactive Elements

- **Form Container** : `<form action method enctype>`
    
- **Labels & Controls** : `<label>`, `<input>`, `<button>`
    
- **Select Inputs** : `<select>`, `<datalist>`, `<optgroup>`, `<option>`
    
- **Text Areas** : `<textarea>`
    
- **Input Types** :  
    `text`, `password`, `email`, `tel`, `url`, `number`, `range`,  
    `date`, `time`, `color`, `file`, `checkbox`, `radio`,  
    `submit`, `image`, `hidden`
    
- **Grouping** : `<fieldset>`, `<legend>`
    
- **Output & Meters** : `<output>`, `<progress>`, `<meter>`
    
- **Validation** : `required`, `pattern`, `min`, `max`, `step`
    
- **Custom UI** : floating labels, styled controls
    
- **Form Accessibility** : `aria-describedby`, `aria-invalid`
    

---

## 🔹 1.7 Global Attributes & Accessibility

- **Common Attributes** : `id`, `class`, `style`, `title`, `hidden`
    
- **Language & Direction** : `lang`, `dir`
    
- **Data Attributes** : `data-*`
    
- **ARIA** : `role`, `aria-*`, `tabindex`
    
- **Best Practices** : semantic tags, contrast, alt text
    
- **Keyboard Access** : focus order, logical tab flow
    
- **Skip Links** : jump to main content
    
- **Landmark Roles** : header, nav, main, footer
    

---

## 🔹 1.8 Scripting, Metadata & Advanced Features

- **Scripting** : `<script>`, `<noscript>`, `type="module"`
    
- **Templates & Slots** : `<template>`, `<slot>`
    
- **Web Components** : custom elements, lifecycle
    
- **Shadow DOM** : encapsulation basics
    
- **Meta Refresh** : redirects, reloads
    
- **Canonical URLs** : `<link rel="canonical">`
    
- **Social Meta** : Open Graph, Twitter Cards
    
- **PWA Manifest** : `<link rel="manifest">`
    
- **Responsive Meta** : viewport, theme-color
    
- **Feature Detection** : Modernizr, polyfills
    

---

If you want, I can next:

- Add **`[[Wiki Links]]`** for each section (e.g., `[[HTML Text Semantics]]`).
    
- Split this into **one note per section** with backlinks.
    
- Or generate a **cheatsheet table** for all HTML elements with brief meanings.